"""
Nudged Elastic Band 

This module allows to run NEB calculations that are parallelized over the images.
For each image a separate job is submitted to the queue. 
"""
__version_info__ = (0,0,1)
__version__ = '.'.join(list(map(str, __version_info__)))

__author__ = ''
